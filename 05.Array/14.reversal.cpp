#include<bits/stdc++.h>
using namespace std;

void reversal(int arr[],int n,int d){
    while (condition)
    {
        /* code */
    }
    
}
int main(){
    int arr[] = {5, 0, 12, 0, 20, 15};
    int n = sizeof(arr) / sizeof(arr[0]);
    reversal(arr,n,d);
    for (int i = 0; i < n; i++) {
        cout << arr[i] << " ";
    }
    return 0;
}